# 1) تهيئة Capacitor
npm install @capacitor/core @capacitor/cli
npx cap init "تحدى نفسك: ألغاز ومنطق" com.abstudio.challengeyourself

# 2) منصة أندرويد
npm install @capacitor/android
npx cap add android
